import 'package:flutter/material.dart';

class TopRatedTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Implement the UI for top-rated movies tab
    return const Center(
      child: Text('Top Rated Movies'),
    );
  }
}